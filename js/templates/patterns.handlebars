<div id='pattern'>
  hogehoge
{{input class="name" type="text" placeholder="name" value=name }}
<button class="submit" {{action addPattern}}>Add</button>
<br />
<table>
{{#each pattern in controller}}
<tr>
  <td class="name">{{pattern.name}}</td>
</tr>
{{/each}}
</table>
</div>
